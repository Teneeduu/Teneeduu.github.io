# 黑洞吞噬 (Black Hole)

Hole.io 风格网页小游戏。React + Vite + Canvas(伪3D) + Three.js(3D原型)。

## 开发
```bash
npm install
npm run dev      # 本地 http://localhost:5173
npm run build    # 产出 dist/
```

## 部署到本仓库(GitHub Pages)
构建后把 `dist/` 内容复制到仓库根的 `game/` 目录，推送即可。
线上地址: https://teneeduu.github.io/game/
